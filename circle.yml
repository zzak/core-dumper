version: 2.0

jobs:
  build:
    docker:
      - image: gcc:latest
    working_directory: ~/work
    steps:
      - checkout
      - run: make
      - run: ulimit -c unlimited
      - run: ./dump
      - run:
          command: ls
          when: always
      - store_artifacts:
          paths: ~/work
